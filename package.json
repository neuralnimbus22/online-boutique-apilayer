{
  "name": "online-boutique-apilayer",
  "version": "0.1.0",
  "description": "Demo API service depending on Postgres and Redis — products + cache + view counter",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
